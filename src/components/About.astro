---
---

<section id="about" class="about-section">
  <div class="container about-container">
    <div class="visual-col">
        <div class="visual-bg"></div>
        <div class="feature-card">
            <h3 class="feature-title">Why Sonberg?</h3>
            <ul class="feature-list">
                <li class="feature-item">
                    <span class="check-icon">✓</span>
                    <p class="feature-text">Data-driven approach to decision making</p>
                </li>
                <li class="feature-item">
                    <span class="check-icon">✓</span>
                    <p class="feature-text">Global network of industry experts</p>
                </li>
                <li class="feature-item">
                    <span class="check-icon">✓</span>
                    <p class="feature-text">Proven track record of ROI improvement</p>
                </li>
            </ul>
        </div>
    </div>
    
    <div class="content-col">
        <h2 class="title">Partnering for Excellence</h2>
        <p class="text-large">
            At Sonberg Consulting, we believe that true transformation starts from within. Our team of seasoned strategists works alongside your leadership to identify opportunities, overcome obstacles, and build a resilient future.
        </p>
        <p class="text-regular">
            Founded on principles of integrity and innovation, we have helped over 500+ enterprises navigate the shifting landscapes of the modern economy.
        </p>
        <a href="#contact" class="link-arrow">
            Meet our team &rarr;
        </a>
    </div>
  </div>
</section>

<style>
    .about-section {
        padding-top: 6rem;
        padding-bottom: 6rem;
        position: relative;
    }

    .about-container {
        display: grid;
        gap: 4rem;
        align-items: center;
    }

    @media(min-width: 768px) {
        .about-container {
            grid-template-columns: 1fr 1fr;
        }
        
        /* Order manipulation for desktop to match original design */
        .visual-col {
            order: 1; /* Was order-1 in original, which means it appears first visually if flex/grid ordered? 
                       Wait, original was:
                       order-2 md:order-1 for visual (left on desktop, right on mobile usually?) 
                       order-1 md:order-2 for text (right on desktop)
                       
                       Actually original: 
                       mobile: text (order-1), visual (order-2) -> text top, visual bottom
                       desktop: visual (order-1), text (order-2) -> visual left, text right
                    */
            order: 1;
        }
        .content-col {
            order: 2;
        }
    }
    
    /* Mobile order */
    .visual-col { order: 2; }
    .content-col { order: 1; }


    .visual-col {
        position: relative;
    }

    .visual-bg {
        position: absolute;
        top: -1rem;
        left: -1rem;
        width: 100%;
        height: 100%;
        background-color: rgba(212, 175, 55, 0.1);
        border-radius: 0.5rem;
        z-index: -10;
    }

    .feature-card {
        background-color: var(--color-navy-800);
        padding: 2rem;
        border-radius: 0.5rem;
        border: 1px solid #233554;
    }

    .feature-title {
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        color: var(--color-gold-500);
    }

    .feature-list {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .feature-item {
        display: flex;
        align-items: flex-start;
        gap: 1rem;
    }

    .check-icon {
        color: var(--color-gold-500);
        margin-top: 0.25rem;
    }

    .feature-text {
        color: var(--color-slate-300);
        margin: 0;
    }

    .title {
        font-size: 1.875rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        color: var(--color-slate-100);
        line-height: 1.2;
    }

    @media(min-width: 768px) {
        .title {
            font-size: 2.25rem;
        }
    }

    .text-large {
        color: var(--color-slate-300);
        margin-bottom: 1.5rem;
        font-size: 1.125rem;
    }

    .text-regular {
        color: var(--color-slate-300);
        margin-bottom: 2rem;
    }

    .link-arrow {
        color: var(--color-gold-500);
        font-weight: 500;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
    }

    .link-arrow:hover {
        text-decoration: underline;
        text-underline-offset: 4px;
        text-decoration-thickness: 2px;
    }
</style>
